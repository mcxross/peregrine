[package]
name = "peregrine-complexity-rules"
version.workspace = true
edition.workspace = true
description = "Complexity ruleset for Peregrine's Move static analysis engine"

[dependencies]
peregrine-analysis-core = { workspace = true }

[dev-dependencies]
toml = { workspace = true }
